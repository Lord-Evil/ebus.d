# ebus.d

Build instructions

**On 64 bit Linux:**
install dmd
run "Make"

**On Windows**
install dmd and dub
run "dub"

**After**
copy config.json.sample to config.json

Open in browser http://localhost:4445/
